DROP INDEX IF EXISTS candidate_name;
DROP TABLE IF EXISTS candidates;
CREATE TABLE IF NOT EXISTS CANDIDATES
(
  c_id   BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY,
  c_name VARCHAR(64) NOT NULL
);
CREATE UNIQUE INDEX IF NOT EXISTS candidate_name
  ON CANDIDATES(c_name);
  
